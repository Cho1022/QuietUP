<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbarPostRead"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.Material3.Dark.ActionBar" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:orientation="vertical"
            android:padding="12dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- 게시판 -->
            <EditText
                android:id="@+id/inputPostReadType"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="게시판"
                android:enabled="false"
                android:focusable="false"
                android:cursorVisible="false" />

            <!-- 제목 -->
            <EditText
                android:id="@+id/inputPostReadSubject"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:hint="제목"
                android:textSize="16sp"
                android:textStyle="bold"
                android:enabled="false"
                android:focusable="false"
                android:cursorVisible="false" />

            <!-- 작성자 -->
            <EditText
                android:id="@+id/inputPostReadNickname"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:hint="작성자"
                android:enabled="false"
                android:focusable="false"
                android:cursorVisible="false" />

            <!-- 작성일 -->
            <EditText
                android:id="@+id/inputPostReadDate"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:hint="작성일"
                android:enabled="false"
                android:focusable="false"
                android:cursorVisible="false" />

            <!-- 내용 -->
            <EditText
                android:id="@+id/inputPostReadText"
                android:layout_width="match_parent"
                android:layout_height="200dp"
                android:layout_marginTop="8dp"
                android:gravity="top"
                android:hint="내용"
                android:inputType="textMultiLine"
                android:enabled="false"
                android:focusable="false"
                android:cursorVisible="false" />

            <!-- (옵션) 이미지 -->
            <ImageView
                android:id="@+id/imageViewPostRead"
                android:layout_width="match_parent"
                android:layout_height="200dp"
                android:layout_marginTop="12dp"
                android:scaleType="centerCrop"
                android:background="@android:color/darker_gray"
                android:visibility="gone" />
        </LinearLayout>
    </ScrollView>

</LinearLayout>
